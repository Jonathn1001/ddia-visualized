/** Shared control styles — theme-token based (see index.css @theme). */
export const btn =
  'px-2 py-1 rounded border border-line bg-panel text-fg hover:bg-line/40 text-xs font-mono transition-colors';

export const btnPrimary =
  'px-2 py-1 rounded border border-set bg-set/10 text-set hover:bg-set/20 text-xs font-mono transition-colors';

export const inputBox = 'bg-ink border border-line rounded px-1 py-0.5 text-fg';
